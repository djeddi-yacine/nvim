-- Toggle editor modules here, then restart. Language switches live elsewhere.
return {
  theme = true,
  mini = {
    pick = true,
    files = true,
    statusline = true,
    pairs = true,
    icons = true,
    clue = true,
    ai = true,
    surround = true,
  },
}
